# Runtime dependencies for the deployed Streamlit application only.
# Versions are pinned to the exact set validated locally and used to produce
# deploy/artifacts/model_pipeline.joblib. scikit-learn must match the training
# version: unpickling a pipeline under a different minor version is unsupported.
# Development/testing extras live in ../requirements-dev.txt.

streamlit==1.60.0
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
joblib==1.5.3
matplotlib==3.11.1
