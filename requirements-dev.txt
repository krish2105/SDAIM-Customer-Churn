# Local development, analysis, training and testing dependencies.
# Runtime-only dependencies for the deployed application live in deploy/requirements.txt.
# Python 3.11 is the supported runtime.

pandas
numpy
scikit-learn
joblib
matplotlib
streamlit

# Statistical tests for drift detection (chi-squared)
scipy

# Experiment tracking and model registry (H2-1). Development only —
# deliberately absent from deploy/requirements.txt so the runtime image stays
# small. A test asserts the application never imports it.
mlflow

# Testing
pytest

# Notebook generation and execution (notebooks/01_eda_and_modeling.ipynb)
nbformat
nbclient
ipykernel
